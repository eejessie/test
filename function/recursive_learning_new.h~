#ifndef _RECURSIVE_LEARNING_NEW_H
#define _RECURSIVE_LEARNING_NEW_H


#include <iostream>
#include <vector>
#include <map>
#include <string>
#include "../class/CircuitNode.h"
#include "../class/FaultList.h"



void recursive_learning_new(map<int, CircuitNode> &masterNodeList, FaultList wt_error, int current_node, int r, int rmax, map<int, int> &MA_act, map<int, int> &MA_obs, map<int, int> &MA_forced, int &consist_flag, int flag_ao, int &inconsist_gate);


#endif
