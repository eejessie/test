#ifndef _BUILD_AND_SHARE_H
#define _BUILD_AND_SHARE_H


#include <iostream>
#include <vector>
#include <map>
#include <string>
#include "class/CircuitNode.h"

void find_common_CMA(map<int, map<int, int> > CMA, map<map<int, int>, int> &times_CMA, int &base_gate);
int build_AND_share(map<int, CircuitNode> &masterNodeList, map<int, map<int, int> > CMA, map<int, map<int, int> > &new_CMA, int &maxLineNumber);

#endif
