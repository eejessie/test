#ifndef _IDTF_MA_NEW_H
#define _IDTF_MA_NEW_H


#include <iostream>
#include <vector>
#include <map>
#include <string>
#include "class/CircuitNode.h"
#include "class/FaultList.h"


int idtf_MA_new(map<int, CircuitNode> &orNodeList, map<int, CircuitNode> &masterNodeList, FaultList &wt_error, map<int, int> &MA, map<int, map<int, int> > &path_values, map<int, int> &error_node_set, multimap<int, vector<int> > &path_set);

#endif
