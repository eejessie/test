#ifndef _UPD_CIRCUIT_H
#define _UPD_CIRCUIT_H


#include <iostream>
#include <vector>
#include <map>
#include <string>
#include "../class/CircuitNode.h"
#include "../class/FaultList.h"

int upd_circuit(map<int, CircuitNode> &orNodeList, map<int, int> &ns_set, map<int, int> &nd_set, int maxLineNumber);

#endif
